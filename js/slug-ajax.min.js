// JavaScript Document
jQuery(document).ready(function($) {
	$( "#ajax-trigger" ).click(function() {
		console.log( 'clicked!' );
		$.get(
			jp.ajaxURL, {
				'action': 'jp_ajax_test',
				'nonce' : jp.nonce,
				'post_id' : post_id,
			},
			function( response ) {
				$( '#ajax-target' ).append( response );
			}
		);
	});
	
	// Now get Post ID from Wordpress
	$( "#get-date" ).click(function() {
		var post_id =$(  '#get-date' ).attr( 'post_id' );
		$.get(
			jp.ajaxURL, {
				'action': 'jp_get_date',
				'nonce' : jp.nonce,
				'post_id' : post_id,
			},
			function( response ) {
				$( '#date' ).append( response );
				  }
				 );
	}
			);
}
					   );